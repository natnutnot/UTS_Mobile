<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BiografiMhs">


        <!-- Activity utama -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity tambahan -->
        <activity android:name=".One" android:exported="true" />
        <activity android:name=".Two" android:exported="true" />
        <activity android:name=".Three" android:exported="true" />
        <activity android:name=".Four" android:exported="true" />
        <activity android:name=".Five" android:exported="true" />

    </application>

</manifest>